# Desarrollo local — el proxy de Vite (vite.config.mts) redirige /api/* a localhost:8000
# Dejar en blanco deja activo el proxy. Poner una URL absoluta la deshabilita.
# Para testear contra el backend de producción: VITE_API_URL=https://proyecto-dosis-ya.vercel.app
VITE_API_URL=
